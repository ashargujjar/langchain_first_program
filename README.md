This is a simple langchain program that takes the input via rest api route "localhost:/30000/chat" sending the body message.
In this the Langcain agents,tools calling is used using the open ai model of chat gpt.
In this it gets the string extreact number and call tools based on  the result if student is fail it automatically call another tool and drop that student from the course

